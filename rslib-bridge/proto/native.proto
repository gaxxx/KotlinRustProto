syntax = "proto3";

package Proto;
option java_package = "com.linkedin.android.proto";

service DroidBackendService {
    rpc Open(Str) returns (Resp);
    rpc Save(SaveIn) returns (Resp);
    rpc Get(Str) returns (Str);
}

message Str {
    string val = 1;
}

message SaveIn{
    string key = 1;
    string val = 2;
}

message Resp{
    sint32 ret = 1;
    string msg = 2;
}

message Empty {}
